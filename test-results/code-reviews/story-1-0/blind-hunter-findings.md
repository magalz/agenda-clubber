- **Porta de Banco de Dados Incorreta** — Em `drizzle.config.ts` e `src/db/index.ts`, a porta de fallback da URL de conexão foi alterada para `54322`, o que provavelmente é um erro de digitação da porta padrão `5432`.
- **Testes E2E Omitidos na CI** — O arquivo `.github/workflows/ci.yml` não inclui a execução dos testes do Playwright (`npx playwright test`), invalidando a utilidade da configuração adicionada em `playwright.config.ts` e `e2e/home.spec.ts`.
- **Step de Build Ausente na CI** — O workflow em `.github/workflows/ci.yml` falha em adicionar um step de validação de build (`npm run build`), abrindo brecha para merges que quebram a compilação do Next.js.
- **Remoção Perigosa do @layer base** — Em `src/app/globals.css`, o bloco `@layer base` foi completamente removido. Isso afeta a ordem de especificidade do Tailwind CSS e pode quebrar componentes dependentes (como os do shadcn/ui).
- **Testes de Schema Falsos-Positivos** — Em `src/db/schema/schema.test.ts`, as asserções como `expect(profiles.id).toBeDefined()` testam apenas se as propriedades do objeto Drizzle existem no JavaScript, não cobrindo constraints reais, relacionamentos ou integridade do banco.
- **Silenciamento de Erros em Produção** — No arquivo `src/lib/sentry.ts`, a inicialização do Sentry para `NODE_ENV === 'production'` está com o código real comentado e substituído por um `console.log`, impedindo o rastreamento real de erros no ambiente de produção.
- **Falta de Gatilho de Atualização no updatedAt** — Em `src/db/schema/collectives.ts`, a coluna `updatedAt` possui apenas `.defaultNow()`, sem a funcionalidade automática de atualização nas edições (como `$onUpdateFn()` no Drizzle).
- **Coluna JSONB Sem Validação** — Em `src/db/schema/collectives.ts`, o campo `socialLinks: jsonb('social_links')` não possui nenhum schema de validação (como Zod) ou restrições em nível de banco, permitindo a inserção de dados inconsistentes ou maliciosos.
- **Dependência de Versão Hardcoded em Mocks** — No arquivo `src/lib/test-utils/msw.ts`, o handler da API do Facebook usa uma URL estática com a versão `v19.0` chumbada, tornando os mocks frágeis e suscetíveis a quebras silenciosas em upgrades de API.
- **Bypass de Tipagem no Pool de Conexão** — Em `src/db/index.ts`, a definição de `globalForDb` utiliza um cast direto agressivo `as unknown as { _pgClient?: ... }`, contornando a segurança de tipos do TypeScript na declaração do cliente do banco de dados.