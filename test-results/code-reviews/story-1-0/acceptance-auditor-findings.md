- **Vazamento de Escopo (Schemas extras)** — **AC/Restrição:** Tasks / Subtasks: Database Layer — **Evidência:** `src/db/index.ts:4-6` e `src/db/schema/schema.test.ts:4-5` — **Desvio:** O diff importa e testa schemas para `collective-members` e `artists`, extrapolando o escopo da Story 1.0 que pedia a criação apenas de `profiles` e `collectives`.
- **Contradição de Design (Bordas de 0px)** — **AC/Restrição:** Dev Notes: Design ("1px border defaults") — **Evidência:** `src/app/globals.css:hunk` (`border-width: 0;` no seletor `*`) — **Desvio:** O código implementa bordas com espessura zero, contradizendo a especificação da estética "Line-over-Black" que exige bordas padrão de 1px.
- **Ausência de Políticas de Segurança (RLS)** — **AC/Restrição:** Dev Notes: Security ("RLS must be enabled on all new tables") — **Evidência:** `src/db/schema/collectives.ts` e diff geral — **Desvio:** A tabela `collectives` foi criada, mas não há evidências (migrations ou scripts) ativando o Row Level Security (RLS) para ela.
- **Ausência da Estrutura Feature-based** — **AC/Restrição:** AC 6 / Dev Notes: Architecture — **Evidência:** Diff geral — **Desvio:** Não foi criada a estrutura de diretórios base exigida (`src/features/{auth,artists,calendar}`) para seguir a arquitetura definida.
- **Impossibilidade de auditar versões/dependências** — **AC/Restrição:** AC 1, AC 2, AC 5 — **Evidência:** Ausência de `package.json` no diff — **Desvio:** Não é possível validar as versões exatas exigidas (Next.js 15+, Drizzle 0.45.2, Vitest 4.1.4, Playwright 1.59.1) sem o arquivo de manifesto do projeto.
- **Impossibilidade de auditar variáveis de ambiente** — **AC/Restrição:** AC 4 / Tasks: Project Setup — **Evidência:** Ausência de `.env.local` ou `.env.example` — **Desvio:** Embora justificado por `.gitignore`, a falta de um template (`.env.example`) impede a validação da sincronização com as credenciais do Supabase.