- **Validação Zod no Client-side Ausente** — **AC 6:** Client and server-side validation using Zod — **Evidência:** `src/components/sign-up-form.tsx` — **Desvio:** O formulário utiliza apenas validação nativa do HTML (ex: `required`, `type="email"`), sem implementar a validação Zod no lado do cliente conforme exigido. O Zod está sendo usado apenas na server action.
- **Inconsistência de Estado em Falhas** — **AC 3 / Checklist:** Estado fica consistente? — **Evidência:** `src/features/auth/actions.ts:98` (`catch (err)`) — **Desvio:** Se a criação em `supabase.auth.signUp` suceder mas o `db.insert(profiles)` falhar, o usuário restará no Supabase Auth sem um registro em `profiles` e a função retornará erro sem desfazer a criação do usuário, gerando inconsistência no banco.
- **Vazamento de Escopo (Schemas de Banco)** — **Escopo / Efeitos colaterais** — **Evidência:** `src/db/schema/collectives.ts` e `src/db/index.ts` (imports de `collectives`, `collectiveMembers`, `artists`) — **Desvio:** O diff inclui tabelas que não pertencem à Story 1.1, que deve focar apenas no cadastro base e na tabela `profiles`.
- **Vazamento de Escopo (Ações de Auth Extras)** — **Escopo / Efeitos colaterais** — **Evidência:** `src/features/auth/actions.ts:114` (`signInAction`, `signInSchema`, `signOutAction`) — **Desvio:** O arquivo implementa lógica e schemas para Login e Logout, que não fazem parte dos Critérios de Aceite da Story 1.1 (focada estritamente no Sign-up).